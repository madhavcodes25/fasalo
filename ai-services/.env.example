# Environment
PORT=8000
MONGO_INITDB_ROOT_USERNAME=fasalo
MONGO_INITDB_ROOT_PASSWORD=fasalo
MONGO_INITDB_DATABASE=fasalo

# Placeholder — real keys injected via env vars in deployment
OPENAI_API_KEY=
